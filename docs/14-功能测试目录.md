# 功能测试目录

> 2026-05-19 开发完成，2026-05-20 新电脑验证用。
> 覆盖 Phase 5-8 全部新增功能 + 核心旧功能回归。

---

## 测试环境确认

- [ ] Docker Desktop 已安装，4 个容器全部 healthy（`docker compose ps`）
- [ ] 数据库已导入 `salon_backup_20260519.sql`
- [ ] AI 向量库已重建（`docker exec salon-ai python ai/embed_from_db.py`，或 `docker compose exec ai-python python ai/embed_from_db.py`，两种写法均可）
- [ ] 管理端可登录：admin / admin123
- [ ] 顾客端可访问：http://localhost/h5/login

---

## 一、Phase 5 — Docker 容器化

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 容器健康 | `docker compose ps` | 4 个服务全部 `(healthy)` |
| 2 | Nginx 前端 | 浏览器打开 `http://localhost` | 显示管理端登录页（非 404） |
| 3 | 后端健康 | `curl http://localhost:8080/actuator/health` | `{"status":"UP"}` |
| 4 | AI 健康 | `curl http://localhost:8000/api/ai/health` | `{"status":"ok","vectors":32}` |
| 5 | Swagger | 浏览器打开 `http://localhost:8080/swagger-ui/index.html` | Swagger 页面正常加载 |
| 6 | Nginx API 代理 | `curl http://localhost/api/auth/login` (POST) | 返回 JSON（非 404） |

---

## 二、Phase 6 — 前端响应式

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 桌面端布局 | 1920px 宽度访问管理端 | 左侧 240px 侧边栏，右侧内容区 |
| 2 | 平板端布局 | Chrome DevTools 设为 768px | 侧边栏缩小为 64px 图标模式 |
| 3 | 手机端布局 | Chrome DevTools 设为 375px | 底部标签栏显示，汉堡菜单可用 |
| 4 | 响应式数据列表 | 手机端打开会员列表 | 卡片列表展示（非 table），无横向滚动 |
| 5 | 抽屉筛选 | 手机端打开会员列表，点击筛选 | 底部抽屉弹出筛选面板 |
| 6 | PageHeader | 任意页面查看顶部标题栏 | 桌面端显示面包屑，手机端显示回退箭头 |
| 7 | 触控目标 | 手机端检查 Button 大小 | 所有可点击元素 >= 44px |

---

## 三、Phase 7 — 店务管理

### 3.1 技师看板

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 页面访问 | 管理端菜单 → 技师看板 | 显示所有技师状态卡片（绿/红/黄/灰） |
| 2 | 状态切换 | 点击某技师卡片的状态按钮 → 选 "忙碌" | 卡片变红色，显示状态已变更 |
| 3 | 倒计时显示 | 设为忙碌后，该技师有进行中的服务 | 卡片显示倒计时 |
| 4 | 轮询刷新 | 等待 10 秒 | 卡片自动刷新（无明显闪烁） |
| 5 | Swagger 验证 | `GET /api/technician-status/list` | 返回所有技师状态 JSON |

### 3.2 轮牌排队

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 页面访问 | 管理端菜单 → 轮牌排队 | 显示排队列表 + 空闲技师网格 |
| 2 | 会员入队 | 点击入队 → 选择会员 → 确认 | 排队列表新增一条，排队号递增 |
| 3 | 分配技师 | 点击排队卡片 → 点击空闲技师 → 确认 | 记录变 "已分配"，技师状态自动变 BUSY |
| 4 | 跳过 | 点击某条排队记录的 "跳过" | 状态变 "已跳过" |
| 5 | 取消 | 点击某条排队记录的 "取消" | 状态变 "已取消" |
| 6 | Swagger | `GET /api/service-queue/list` | 返回当前排队列表 |

### 3.3 服务计时器

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 开始计时 | 为某技师开始 30 分钟服务计时 | 倒计时开始，显示 MM:SS |
| 2 | 暂停 | 点击暂停 | 倒计时停止，按钮变 "继续" |
| 3 | 继续 | 点击继续 | 倒计时恢复 |
| 4 | 完成 | 点击完成 | 计时结束，记录实际时长 |
| 5 | 80% 提醒 | 等待至剩余 20% 时间 | 黄色脉冲动画（桌面端 Notification 如授权） |
| 6 | 超时红色 | 倒计时归零后 | 正计时显示红色 +X:XX |
| 7 | Swagger | `GET /api/service-timer/active` | 返回进行中的计时列表 |

### 3.4 财务日结

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 页面访问 | 管理端菜单 → 财务日结 | 显示今日系统汇总（现金/微信/支付宝各渠道金额） |
| 2 | 人工录入 | 填入人工现金/微信/支付宝金额 | 差异行自动计算 |
| 3 | 差异高亮 | 人工金额 ≠ 系统金额 | 差异行红色高亮 |
| 4 | 锁定 | 确认数据无误后点击锁定 | 日结锁定成功，不可再修改 |
| 5 | 历史查询 | 查看历史日结列表 | 分页显示过往日结记录 |
| 6 | Swagger | `GET /api/daily-close/today` | 返回今日汇总数据 |

---

## 四、Phase 7 — 营销增强

### 4.1 标签规则

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 页面访问 | 管理端菜单 → 标签规则 | 规则列表 + 新增按钮 |
| 2 | 创建规则 | 新增规则："last_visit_days > 60 → 流失风险" | 保存成功 |
| 3 | 启用/禁用 | 切换规则开关 | 禁用后不执行 |
| 4 | 手动执行 | 点击 "立即执行" | 匹配的会员被打上对应标签 |
| 5 | 会员列表验证 | 打开会员列表查看被打标会员 | 标签徽标显示在会员卡片上 |

### 4.2 RFM 分层

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 仪表盘饼图 | 打开管理端仪表盘 | RFM 分布饼图卡片显示（8 个分层） |
| 2 | 会员列表徽标 | 打开会员列表 | 每个会员显示 RFM 分层徽标（CHAMPIONS/LOYAL/...） |
| 3 | 定时任务验证 | 查看后端日志 | RfmScheduler 每周一凌晨 2:00 执行 |

### 4.3 生日营销

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 定时任务 | 查看后端日志 | BirthdayScheduler 每天 8:00 检查当天生日会员 |
| 2 | 手动触发 | Swagger 或后端 Debug | 当天生日会员自动发券 + 记录 notification_log |

### 4.4 通知日志

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 发送记录 | 触发任意通知（如 send-code） | `notification_log` 表新增一条记录 |
| 2 | 状态流转 | 发送成功 | status 从 0 → 1 |

---

## 五、Phase 8 — 顾客端 H5

### 5.1 顾客认证

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 发送验证码 | 手机端访问 `/h5/login` → 输入手机号 → 发送验证码 | 提示 "验证码已发送"，按钮变 60s 倒计时 |
| 2 | 发送限流 | 倒计时未结束再次点击 | 60 秒内不可重发；后端返回 429 |
| 3 | 验证码登录 | 输入手机号 + `123456`（Mock SMS 固定验证码） | 登录成功，跳转顾客首页 |
| 4 | 顾客首页 | `/h5` | 显示：问候语 + 积分/余额/优惠券 3 卡 + 快捷入口 |
| 5 | Token 持久化 | 刷新页面 | 仍保持登录态（不需要重新登录） |
| 6 | 退出登录 | 首页点击退出 | 跳回登录页 |
| 7 | Swagger | `POST /api/customer/auth/send-code` → `POST /api/customer/auth/login` | 返回 token，`GET /api/customer/auth/me` 返回会员信息 |

### 5.2 在线预约

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 预约入口 | 顾客首页点击 "在线预约" | 进入 3 步预约向导 |
| 2 | 第一步：选服务 | 选择服务项目 | 显示服务列表（含分类），点击选中 |
| 3 | 第二步：选时间 | 选择日期 + 技师 + 时段 | 日历可用日期可点，时段列表显示可用时段 |
| 4 | 时段冲突检测 | 某个时段已被预约 | 该时段不显示或灰色不可选 |
| 5 | 第三步：确认 | 确认预约信息 → 提交 | 预约创建成功 |
| 6 | 我的预约 | 个人中心查看预约记录 | 新创建的预约显示在列表中 |
| 7 | 取消预约 | 取消一条预约（status=1） | 状态变已取消 |

### 5.3 顾客自助门户

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 个人资料 | `/h5/profile` → 点击个人信息 | 显示姓名/手机/等级/积分/余额 |
| 2 | 消费记录 | 点击 "消费记录" | 分页列表，显示时间/金额/支付方式 |
| 3 | 优惠券 | 点击 "我的优惠券" | 显示可用/已用/过期优惠券 |
| 4 | 次卡 | 点击 "我的次卡" | 显示次卡剩余次数和有效期 |

### 5.4 支付模块（Mock）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 创建支付 | POST `/api/customer/payment/create` {orderId, method:"WECHAT"} | 返回 payId + qrCodeUrl |
| 2 | 查询支付状态 | GET `/api/customer/payment/{id}/status` | 返回 status(0=待支付) |
| 3 | Mock 回调 | POST `/api/payment/callback/wechat` {paymentId, transactionId} | 支付状态变 "已支付" |
| 4 | 支付页面 | 手机端 `/h5/payment?orderId=1` | 显示金额 + 支付方式选择（微信/支付宝/余额） |

### 5.5 CustomerLayout & 底部导航

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 底部标签栏 | 顾客端任意页面 | 底部固定 3 标签：首页/预约/我的 |
| 2 | 标签切换 | 点击标签栏图标 | 对应页面切换，当前标签高亮 |
| 3 | 回退按钮 | 进入子页面（如预约详情） | 顶部标题栏左侧显示回退箭头 |
| 4 | 安全区适配 | iPhone 底部横条机型（如 375x812） | 内容不被底部横条遮挡 |

---

## 六、AI 语义搜索

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | AI 搜索框 | 管理端会员列表页 → AI 搜索框输入 "高消费贵宾" → 搜索 | 返回匹配结果卡片 + 相似度百分比 |
| 2 | 点击跳转 | 点击搜索结果卡片 | 跳转到对应会员详情页 |
| 3 | 无结果 | 搜索无意义词 "xyz123" | 提示无匹配结果 |
| 4 | API | `curl "http://localhost:8000/api/ai/search?q=染发&top_k=3"` | 返回 3 条 JSON，含 member_id + similarity |
| 5 | Java BFF | `GET /api/ai/search?q=流失&top_k=3` (带 admin token) | 返回搜索结果 JSON |

---

## 七、核心旧功能回归（防止新代码破坏）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 管理端登录 | admin / admin123 | 登录成功，跳转仪表盘 |
| 2 | 会员列表 | 管理端菜单 → 会员管理 | 分页列表，高级筛选可用 |
| 3 | 会员详情 | 点击某会员 | 详情页：基本信息 + 消费记录 + 优惠券 + 次卡 |
| 4 | 消费收银 | 管理端菜单 → 消费收银 | 创建消费订单，选择服务/会员/支付/折扣 |
| 5 | 预约管理 | 管理端菜单 → 预约管理 | 预约列表 + 新增/取消/状态变更 |
| 6 | 服务管理 | 管理端菜单 → 服务管理 | 服务项目 CRUD |
| 7 | 员工管理 | 管理端菜单 → 员工管理 | 员工 CRUD + 排班 |
| 8 | 优惠券管理 | 管理端菜单 → 优惠券 | 优惠券 CRUD + 发放 |
| 9 | 库存管理 | 管理端菜单 → 库存管理 | 库存记录 CRUD |
| 10 | 会员等级 | 管理端菜单 → 会员等级 | 等级列表 + 折扣规则 |
| 11 | 报表 | 管理端菜单 → 报表 | 营收/会员/服务报表正常显示 |
| 12 | 数据大屏 | 管理端菜单 → 数据大屏 | 实时数据展示 |
| 13 | 管理员管理 | 管理端菜单 → 管理员 | 管理员 CRUD（仅 admin 角色可操作） |

---

## 八、安全与限流

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 未登录访问 | 不登录直接访问 `http://localhost:8080/api/member/page` | 返回 401 `{"code":401,"msg":"未登录或Token已过期"}` |
| 2 | 无权限操作 | 非 admin 角色尝试创建管理员 | 返回 403 `{"code":403,"msg":"无权执行此操作"}` |
| 3 | 登录限流 | 连续 6 次错误密码登录（60 秒内） | 第 6 次返回 429 |
| 4 | 验证码限流 | 60 秒内 2 次请求 send-code | 第 2 次返回 429 |
| 5 | Swagger 生产关闭 | 检查 `application-prod.yml` | `springdoc.swagger-ui.enabled=false` |

---

## 九、移动端完整用户旅程（端到端）

> 在 Chrome DevTools 375px 宽度下完成以下完整流程：

- [ ] `/h5/login` → 输入手机号 `13800138000` → 发送验证码 → 输入 `123456` → 登录成功
- [ ] 首页显示积分/余额/优惠券
- [ ] 点击预约 → 选服务 → 选日期+技师+时段 → 确认提交
- [ ] 个人中心查看预约记录（新预约可见）
- [ ] 个人中心查看消费记录
- [ ] 退出登录 → 回到登录页
- [ ] 底部标签栏全程可见，切换正常

---

## 十、Phase 8 新增功能（2026-05-23）

### 10.1 在线充值/购卡（I5）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | Admin 充值 | POST /api/member/{id}/recharge { amount, payMethod } | 200 + 余额增加 + recharge_record 插入 |
| 2 | H5 充值页面 | /h5/recharge → 选金额 → 确认充值 | 页面显示充值成功，余额更新 |
| 3 | H5 我的次卡 | /h5/service-cards | 显示已购次卡列表，含剩余次数 |
| 4 | H5 次卡详情 | 点击某次卡 | 显示次卡详情（服务项目、有效期、剩余次数） |
| 5 | H5 购买次卡 | 次卡详情页 → 购买 | 200 + 扣余额 + member_card 记录创建 |

### 10.2 服务进度查看（I6）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | IDLE 状态 | 无进行中服务的会员访问 /h5/progress | 显示"当前无进行中的服务"+ 去预约按钮 |
| 2 | IN_SERVICE 状态 | 有计时中的会员访问 /h5/progress | 显示计时圆圈 + 技师姓名 + 服务项目 |
| 3 | 暂停状态 | 技师暂停计时后会员刷新 | 显示"已暂停" + 计时圆圈 paused 样式 |
| 4 | 计时进度 | 观察倒计时分钟数 | 实时更新（前端每秒轮询） |

### 10.3 挂单/取单（B9）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 挂单 | 收银页 → 选会员+服务 → 点击"挂单" | 200 + 订单 status=0，不扣余额/积分 |
| 2 | 挂单列表 | GET /api/consumption/suspended | 显示所有 status=0 的挂单 |
| 3 | 取单结算 | 挂单列表 → 选一条 → 点击取单 → 选择支付方式 → 确认 | 200 + status 0→1 + 余额/积分扣减 |
| 4 | 取单优惠券 | 挂单取单时输入优惠券码 | 200 + 优惠券核销 + 金额抵扣 |
| 5 | 取单组合支付 | 挂单取单时选微信+余额 | 200 + 两笔 payment_detail |

### 10.4 采购订单（G7）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 创建采购单 | 库存管理 → 采购订单Tab → 新增 → 选供应商+商品 → 保存 | 200 + 订单 status=0 + 编号 PO-yyyyMMdd-xxxxx |
| 2 | 提交审核 | 草稿 → 点击提交 | status 0→1 |
| 3 | 审核通过 | 已提交 → 点击审核 | status 1→2 + approver + approved_time |
| 4 | 确认收货 | 已审核 → 点击收货 | status 2→3 + 库存自动增加 + stock_record 入库流水 |
| 5 | 取消 | 草稿/已提交 → 取消 | status →4 |
| 6 | 状态守卫 | 对 status≠0 的订单点删除 | 400 "只有草稿状态可删除" |
| 7 | 收货库存 | 收货后查商品库存 | stock_qty 增加对应数量 |

### 10.5 退货管理（G8）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 创建退货单 | 库存管理 → 退货管理Tab → 新增 → 选供应商+商品 → 保存 | 200 + 订单 status=0 + 编号 RO-yyyyMMdd-xxxxx |
| 2 | 提交审核 | 草稿 → 提交 | status 0→1 |
| 3 | 审核通过 | 已提交 → 审核 | status 1→2 |
| 4 | 驳回 | 已提交 → 驳回 | status 1→4 |
| 5 | 完成退货 | 已审核 → 完成 | status 2→3 + 库存自动扣减(H1守卫) + stock_record 退货流水 |
| 6 | 库存不足 | 退货数量 > 库存 → 完成退货 | 400 "库存不足" |
| 7 | 状态守卫 | 对非草稿订单点编辑 | 400 "只有草稿状态可编辑" |

### 10.6 自动提成结算（F5）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 生成结算 | POST /api/commission/settlements { periodStart, periodEnd } | 200 + 按技师汇总订单数和提成总额 |
| 2 | 重复生成 | 同一周期再生成 | 400 "该周期已存在结算" |
| 3 | 确认 | 草稿 → PUT /{id}/confirm | status 0→1 + confirmed_at |
| 4 | 标记已付 | 已确认 → PUT /{id}/pay | status 1→2 + paid_at |
| 5 | 删除 | 草稿 → DELETE | 200 |
| 6 | 状态守卫 | 对已确认的点删除 | 400 "状态不符" |
| 7 | 详情含订单 | GET /{id} | items 数组含该周期内该技师所有消费订单 |

### 10.7 完成通知下一位（E8）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 计时完成 | 技师对排队顾客完成服务计时 | timer status→3 + 技师释放(AVAILABLE) |
| 2 | 自动创建订单 | 服务计时完成 | consumptionOrder 自动创建（serviceItem+member） |
| 3 | 事件发布 | 计时完成 | TimerCompletedEvent 发布（queueId, employeeId, memberId） |
| 4 | 排队分配联动 | 完成事件触发 → 下一位排队顾客 | ⚠ 通知渠道（SMS/推送）需外部服务，见 docs/13 §5.3 |

### 10.8 积分商城（D10）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | Admin 新增商品 | /api/points/products POST | 200 + 商品创建 |
| 2 | Admin 编辑商品 | PUT /api/points/products/{id} | 200 + 字段更新 |
| 3 | Admin 上架/下架 | PUT /{id}/status?status=0 | 200 + 幂等（重复设置同状态不报错） |
| 4 | H5 商品列表 | GET /api/customer/portal/points/products | 只返回 status=1 且 stock>0 |
| 5 | H5 兑换 | POST /api/customer/portal/points/exchange { productId, quantity } | 200 + 库存-1 + 积分扣减 + 记录创建 |
| 6 | 积分不足 | 兑换时所需积分 > 会员积分 | 400 "会员积分不足" |
| 7 | 库存不足 | 兑换数量 > 库存 | 400 "商品库存不足" |
| 8 | Admin 兑换记录 | GET /api/points/exchange-records | 分页列表 |
| 9 | Admin 标记领取 | PUT /{id}/claim | status 0→1 |
| 10 | 并发兑换 | 两个请求同时兑换最后一件商品 | 一个成功，一个 400（原子扣减 H1 守卫） |

---

## 测试结果记录（待执行 — 2026-05-23）

| 章节 | 通过/失败 | 备注 |
|------|----------|------|
| 一、Docker | 待执行 | |
| 二、响应式 | 待执行 | |
| 三、店务管理 | 待执行 | |
| 四、营销增强 | 待执行 | |
| 五、顾客端 H5 | 待执行 | |
| 六、AI 搜索 | 待执行 | |
| 七、旧功能回归 | 待执行 | |
| 八、安全限流 | 待执行 | |
| 九、端到端 | 待执行 | |
| 十、Phase 8 新增 | 待执行 | I5/I6/B9/G7/G8/F5/E8/D10 共 8 功能 48 测试用例 |
