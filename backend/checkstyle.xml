<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    美发沙龙管理系统 — Checkstyle 配置
    基于 Google Java Style Guide

    渐进式策略：
    - Phase 1（当前）：核心规则，违规 = 构建失败
    - Phase 2（后续）：严格规则，按文件逐步启用
-->

<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java, properties, xml"/>

    <module name="BeforeExecutionExclusionFileFilter">
        <property name="fileNamePattern" value="module\-info\.java$"/>
    </module>

    <!-- ========== Phase 1：核心规则（必须通过） ========== -->

    <!-- 禁止 Tab 字符 -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- 行尾多余空白 -->
    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="message" value="行尾有多余空白"/>
    </module>

    <!-- 文件末尾必须有空行 -->
    <module name="NewlineAtEndOfFile"/>

    <module name="TreeWalker">

        <!-- === 导入：未使用/冗余 === -->
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <!-- AvoidStarImport 关闭：项目使用 Spring Boot 惯例 import xxx.* -->

        <!-- === 命名规范 === -->
        <module name="ConstantName"/>
        <module name="LocalFinalVariableName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="MethodName"/>
        <module name="PackageName"/>
        <module name="ParameterName"/>
        <module name="StaticVariableName"/>
        <module name="TypeName"/>

        <!-- === 禁止 System.out / printStackTrace === -->
        <module name="RegexpSinglelineJava">
            <property name="format" value="System\.(out|err)\.print(ln|f)?\("/>
            <property name="message" value="禁止 System.out，请使用 Slf4j"/>
        </module>
        <module name="RegexpSinglelineJava">
            <property name="format" value="\.printStackTrace\(\)"/>
            <property name="message" value="禁止 printStackTrace()，请使用 log.error(msg, e)"/>
        </module>

        <!-- === 禁止空 catch 块 === -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignored"/>
        </module>

        <!-- === switch 必须有 default === -->
        <module name="MissingSwitchDefault"/>

        <!-- === @Override 注解 === -->
        <module name="MissingOverride"/>

        <!-- === long 字面量用大写 L === -->
        <module name="UpperEll"/>

        <!-- === 简化布尔表达式 === -->
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>

        <!-- === 数组类型风格：String[] args === -->
        <module name="ArrayTypeStyle"/>

        <!-- === 修饰符顺序 === -->
        <module name="ModifierOrder"/>
        <module name="RedundantModifier"/>

        <!-- === 禁止空代码块 === -->
        <module name="EmptyBlock"/>

        <!-- ================================================ -->
        <!-- Phase 2：严格规则（按文件逐步启用，去掉注释即生效） -->
        <!-- ================================================ -->

        <!--
        <module name="NeedBraces"/>
        <module name="AvoidNestedBlocks"/>
        <module name="MagicNumber">
            <property name="ignoreNumbers" value="-1, 0, 1, 2"/>
            <property name="ignoreHashCodeMethod" value="true"/>
            <property name="ignoreAnnotation" value="true"/>
            <property name="ignoreFieldDeclaration" value="true"/>
        </module>
        <module name="AvoidInlineConditionals"/>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>
        <module name="WhitespaceAround"/>
        <module name="MethodParamPad"/>
        <module name="ParenPad"/>
        <module name="NoWhitespaceBefore"/>
        <module name="GenericWhitespace"/>
        <module name="CustomImportOrder">
            <property name="customImportOrderRules"
                value="STATIC###STANDARD_JAVA_PACKAGE###THIRD_PARTY_PACKAGE###SPECIAL_IMPORTS"/>
            <property name="standardPackageRegExp" value="^(java|javax)\."/>
            <property name="thirdPartyPackageRegExp" value=".*"/>
            <property name="specialImportsRegExp" value="^com\.salon\."/>
            <property name="sortImportsInGroupAlphabetically" value="true"/>
            <property name="separateLineBetweenGroups" value="true"/>
        </module>
        <module name="AnnotationLocation"/>
        -->

    </module>
</module>
